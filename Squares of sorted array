class Solution {
    public int[] sortedSquares(int[] nums) {
        int a=nums.length;
        for(int i=0;i<a;i++){
            nums[i]=nums[i]*nums[i];
        }
        Arrays.sort(nums);
        return nums;

        
    }
}
